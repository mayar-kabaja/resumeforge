# Note: Render is no longer free
# This file is kept for reference only
# Please use PythonAnywhere (recommended) or Railway for free hosting
# See README.md for detailed deployment instructions

services:
  - type: web
    name: resumeforge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: AI_PROVIDER
        value: groq
      - key: GROQ_API_KEY
        sync: false
